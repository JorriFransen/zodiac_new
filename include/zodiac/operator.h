#pragma once

namespace Zodiac
{
    enum Binary_Operator
    {
        BINOP_INVALID,

        BINOP_ADD,
        BINOP_SUB,
    };

    enum Unary_Operator
    {
        UNOP_INVALID,

        UNOP_DEREF,
    };
}
