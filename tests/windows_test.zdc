
windows :: import windows;

mainCRTStartup :: func ()
{
    exit_code := main();
    @exit(exit_code);
}

main :: func () -> s64
{
    stdout_handle := windows.GetStdHandle(@cast(s32, windows.WINDOWS_STDOUT_HANDLE_NUM));

    return 41;
}

#foreign #noreturn ExitProcess :: func (exit_code: u32) -> void
