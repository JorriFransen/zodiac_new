

std :: import std;

main :: func() -> s64
{
    std.print("redeclaration test\n");

    x : s64;
    x : s64; // Illegal

    y : float;
    {
        y : float; // Shadowing is also illegal
    }

    func1();

    return 0;
}

func1 :: func() -> void { return; }
func1 :: func() -> void { return; }

#naked #noreturn _start :: func ()
{
    posix_call_main_and_exit();
}

#noreturn posix_call_main_and_exit :: func ()
{
    exit_code := main();
    @exit(exit_code);
}

