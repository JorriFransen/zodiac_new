
// std :: import std;

main :: func() -> s64 {
    // std.print("Hello, World! from multiple platforms...\n");

    // std.print("Returning: ");
    // std.print_num(42);
    // std.print("\n");

    return add(40, 2);
}

add :: func(a: s64, b: s64) -> s64
{
    return a + b;
}
